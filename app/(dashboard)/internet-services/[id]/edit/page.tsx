import { InvoiceForm } from "@/components/pages/FormPages"; export default async function Page({ params }: { params: Promise<{ id: string }> }){ const { id } = await params; return <InvoiceForm edit id={id} />; }