import { LeadForm } from "@/components/pages/FormPages"; export default async function Page({ params }: { params: Promise<{ id: string }> }){ const { id } = await params; return <LeadForm edit id={id} />; }