# Public API endpoint (relative path if using proxy or full URL)
NEXT_PUBLIC_API=/api/v1
NEXT_PUBLIC_API_PROVIDER=dekadata

# Backend API URL for server-side proxy
BACKEND_API_URL=https://api.example.com
